en:
  issue_view:
    list:
      link: 'Issue view list'
    marker:
      never_seen: 'Never opened this issue'
      seen_but_not_all: 'Saw this issue, but not all changes and comments'
      seen_all: 'Saw all changes in this issue'
    settings:
      label:
        record_only_last: 'Record only last view (not all of them)'
        record_watchers: 'Record watchers (if not checked then record only author and assignee)'
        wipe_multiple: 'Delete all records but the last one for each pair issue-user'
